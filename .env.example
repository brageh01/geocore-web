# Copy to .env.local and fill in. .env.local is gitignored — never commit real keys.

# Google Maps Platform key for Photorealistic 3D Tiles. Enable the "Map Tiles API"
# in Google Cloud Console → APIs & Services → Credentials. Shipped in the client
# bundle by necessity, so restrict it by HTTP referrer and to the Map Tiles API only.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# Cesium Ion access token, for Ion-hosted assets. Create at https://ion.cesium.com/tokens
# Also exposed to the client bundle.
NEXT_PUBLIC_CESIUM_ION_TOKEN=

# NASA FIRMS MAP_KEY for the active-fire CSV API. Request at
# https://firms.modaps.eosdis.nasa.gov/api/map_key/ — server-side only.
NASA_FIRMS_API_KEY=

# OpenAQ v3 API key, sent as the X-API-Key header. Register at
# https://explore.openaq.org/register — server-side only.
OPENAQ_API_KEY=
